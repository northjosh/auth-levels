name: send_push
description: Sends the auth-levels contract's FCM message to one device token.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.7.0
  googleapis_auth: ^2.0.0
  http: ^1.2.0
  uuid: ^4.5.0

dev_dependencies:
  lints: ^6.0.0
