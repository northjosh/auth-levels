name: stub_server
description: In-memory stand-in for the auth-levels backend, per .scratch/mobile-companion-app/api-contract.md.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.3

dependencies:
  args: ^2.7.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
